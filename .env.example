# Firebase Configuration for Static Export (Spark Plan Compatible)

# App URL - Your Firebase Hosting URL
# Example: https://your-project.web.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Development Firebase Functions URL (optional - only needed if not using emulator)
# For local development with Firebase emulator, you don't need this
# NEXT_PUBLIC_FIREBASE_FUNCTIONS_DEV_URL=http://localhost:5001/letpractice/us-central1

# Production uses Firebase Hosting rewrites (/api/*) - no extra config needed!
# The firebase.json already has rewrites configured:
# - /api/daily-task -> dailyTask function
# - /api/events -> events function  
# - /api/download -> download function
# - /api/apk -> apk function
